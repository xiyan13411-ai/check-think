export default function LabAssetsPage() {
  return (
    <main className="min-h-screen bg-stone-50 px-4 py-8 text-stone-700">
      <div className="mx-auto max-w-md rounded-2xl bg-white p-5 shadow-sm">
        <h1 className="text-lg font-bold">Asset Lab</h1>
        <p className="mt-2 text-sm text-stone-500">
          This legacy lab page is disabled while the main hero uses the new runtime asset registry.
        </p>
      </div>
    </main>
  );
}
